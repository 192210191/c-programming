#include<stdio.h>
#include<string.h>
int main()
{
	char str[]="sauui";
	int i,l;
	int flag=0;
	l=strlen(str);
	for(i=0;i<l;i++)
	{
		if(str[i]!=str[l-i-1])
		{
			flag=1;
			break;
		}
	}
	if(flag)
	printf("palindrome");
	else
	printf("not a palindrome");
	return 0;
}
